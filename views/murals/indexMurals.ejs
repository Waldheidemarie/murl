<% include ../partials/header %>

  <header class="jumbotron jumbotron-fluid">
    <div class="container">
      <h1 class="display-4">mürl</h1>
      <p class="lead">
        browse through or be the first to post artwork
      </p>
      <hr class="my-4" />
      <p class="text-muted">sign up for a free account or login to add images and details</p>
      <br>
      <button type="submit" class="btn btn-dark btn-lg">add new</button>
    </div>
  </header>

  <div class="container">
    <div class="card-columns">
      <!-- <div class="row"> -->
        <% murals.forEach(mural => { %> 
          <!-- <div class="cardbox col-md-4 col-sm-6"> -->
            <div class="card bg-dark">
              <img class="card-img-bottom" src="<%= mural.image %>" alt="" />
              <div class="card-img-overlay">
                <p class="card-text"><%= mural.city %></p>
              </div>
            </div>
          <!-- </div> -->
        <% }); %>
      <!-- </div> -->
    </div>
  </div>

<% include ../partials/footer %>
